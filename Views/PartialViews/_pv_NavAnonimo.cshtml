@* Navbar para usuarios que aún no han iniciado sesión *@
<nav class="bg-white border-b border-gray-100 shadow-sm mb-6">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
            @* Logo / Brand *@
            <div class="flex items-center">
                <a class="text-2xl font-black text-blue-600 tracking-tight hover:opacity-80 transition-all"
                    asp-controller="Home" asp-action="Index">
                    Consorcio App
                </a>
            </div>

            @* Navigation Links *@
            <div class="flex items-center space-x-8">
                <a class="text-sm font-medium text-gray-500 hover:text-blue-600 transition-colors" asp-controller="Home"
                    asp-action="Index">Inicio</a>
                <a class="text-sm font-medium text-gray-500 hover:text-blue-600 transition-colors" asp-controller="Home"
                    asp-action="Ingresar">Ingresar</a>
                <a class="btn-principal text-sm px-5 py-2 rounded-lg" asp-controller="Home"
                    asp-action="Registrar">Registrarse</a>
            </div>
        </div>
    </div>
</nav>